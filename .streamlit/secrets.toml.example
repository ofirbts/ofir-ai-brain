# Copy to secrets.toml and fill in your values.
# For Streamlit Cloud: paste this format into the Secrets field in App settings.
#
# Format (TOML - use quotes for strings):
GOOGLE_API_KEY = "your-google-ai-studio-key"

# Optional - for Drive sync:
# OFIR_BRAIN_FOLDER_ID = "your-drive-folder-id"
# GOOGLE_APPLICATION_CREDENTIALS = "/path/to/credentials.json"
